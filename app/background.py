def safe_check_watchlist_changes(symbols):
    """
    Placeholder for background watchlist checks. Real logic can be added later.
    Returns a list of text notifications; never raises in production.
    """
    try:
        # TODO: plug real checks (price thresholds, news, filings, etc.)
        return []
    except Exception:
        return []
